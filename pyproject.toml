[project]
name = "my-fastapi-app"
version = "0.1.0"
description = "My FastAPI Application"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.111.0",
]

[tool.fastapi]
# Optional: Define the entrypoint if your file structure is non-standard
# entrypoint = "main:app"   